---
phase: 02-architecture-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - LogViewer/IDispatcher.cs
  - LogViewer/WpfDispatcher.cs
  - LogViewer/LogControlViewModel.cs
  - LogViewer/LogControl.xaml.cs
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04
  - BUG-02

must_haves:
  truths:
    - "IDispatcher interface exists with CheckAccess(), Invoke<T>(Func<T>), InvokeAsync(Action), InvokeAsync<T>(Func<T>)"
    - "WpfDispatcher implements IDispatcher by delegating to System.Windows.Threading.Dispatcher"
    - "LogControlViewModel constructor accepts IDispatcher instead of Dispatcher; no direct reference to System.Windows.Threading.Dispatcher in its field or constructor parameter"
    - "LogControl.xaml.cs constructs WpfDispatcher(Dispatcher) and passes it to LogControlViewModel"
    - "_pauseBuffer never grows beyond MaxLogSize: OnLogEventAsync drops entries when _pauseBuffer.Count >= MaxLogSize under the lock"
    - "All 114 existing tests continue to pass"
  artifacts:
    - path: "LogViewer/IDispatcher.cs"
      provides: "Dispatcher abstraction interface"
      contains: "public interface IDispatcher"
    - path: "LogViewer/WpfDispatcher.cs"
      provides: "WPF Dispatcher wrapper"
      contains: "public class WpfDispatcher : IDispatcher"
    - path: "LogViewer/LogControlViewModel.cs"
      provides: "Updated ViewModel with IDispatcher and BUG-02 fix"
      contains: "IDispatcher"
    - path: "LogViewer/LogControl.xaml.cs"
      provides: "Wire: constructs WpfDispatcher and passes to ViewModel"
      contains: "WpfDispatcher"
  key_links:
    - from: "LogViewer/LogControl.xaml.cs"
      to: "LogViewer/WpfDispatcher.cs"
      via: "new WpfDispatcher(Dispatcher)"
      pattern: "new WpfDispatcher"
    - from: "LogViewer/LogControlViewModel.cs"
      to: "LogViewer/IDispatcher.cs"
      via: "private readonly IDispatcher _dispatcher"
      pattern: "IDispatcher _dispatcher"
---

<objective>
Create the IDispatcher abstraction and WpfDispatcher implementation, update LogControlViewModel to consume IDispatcher, wire LogControl to pass a WpfDispatcher instance, and fix the unbounded pause buffer (BUG-02). These four changes are independent of the LogColor work (Plan 01) so they can run in the same wave.

Purpose: After this plan, LogControlViewModel can be instantiated in unit tests by passing a mock IDispatcher — no WPF runtime required. This unblocks Phase 3's TEST-01 through TEST-03.

Output: Two new files (IDispatcher.cs, WpfDispatcher.cs); two modified files (LogControlViewModel.cs, LogControl.xaml.cs).
</objective>

<execution_context>
@C:/Users/Shady/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Shady/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Shady/source/repos/LogViewer/.planning/PROJECT.md
@C:/Users/Shady/source/repos/LogViewer/.planning/ROADMAP.md
@C:/Users/Shady/source/repos/LogViewer/.planning/STATE.md
</context>

<interfaces>
<!-- New types and changed signatures — read before editing any file -->

IDispatcher.cs — target shape:
  namespace LogViewer
  public interface IDispatcher
    bool CheckAccess();
    T Invoke&lt;T&gt;(Func&lt;T&gt; callback);
    Task InvokeAsync(Action callback);
    Task&lt;T&gt; InvokeAsync&lt;T&gt;(Func&lt;T&gt; callback);

WpfDispatcher.cs — target shape:
  namespace LogViewer
  public sealed class WpfDispatcher : IDispatcher
    private readonly System.Windows.Threading.Dispatcher _dispatcher;
    public WpfDispatcher(System.Windows.Threading.Dispatcher dispatcher) — ArgumentNullException.ThrowIfNull
    public bool CheckAccess() => _dispatcher.CheckAccess();
    public T Invoke&lt;T&gt;(Func&lt;T&gt; callback) => _dispatcher.Invoke(callback);
    public async Task InvokeAsync(Action callback) => await _dispatcher.InvokeAsync(callback);
    public async Task&lt;T&gt; InvokeAsync&lt;T&gt;(Func&lt;T&gt; callback) => await _dispatcher.InvokeAsync(callback);

LogControlViewModel.cs — changes:
  Field:  private readonly IDispatcher _dispatcher;         (was: Dispatcher)
  Constructor param: IDispatcher dispatcher                 (was: Dispatcher dispatcher)
  Guard:  _dispatcher = dispatcher ?? throw new ArgumentNullException(nameof(dispatcher));  (unchanged logic)
  DispatchIfNecessary&lt;T&gt;(Func&lt;T&gt;):   calls _dispatcher.CheckAccess(), _dispatcher.Invoke(callback)
  DispatchIfNecessaryAsync(Action):    calls _dispatcher.CheckAccess(), await _dispatcher.InvokeAsync(callback)
  DispatchIfNecessaryAsync&lt;T&gt;(Func&lt;T&gt;): calls _dispatcher.CheckAccess(), await _dispatcher.InvokeAsync(callback)
  OnLogEventAsync BUG-02 fix — inside lock (_pauseLock) before _pauseBuffer.Add(e):
    if (_pauseBuffer.Count >= MaxLogSize) return;
    _pauseBuffer.Add(e);

LogControl.xaml.cs — change:
  In constructor, line: _viewModel = new LogControlViewModel(Dispatcher);
  Change to:           _viewModel = new LogControlViewModel(new WpfDispatcher(Dispatcher));
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create IDispatcher and WpfDispatcher</name>
  <files>LogViewer/IDispatcher.cs, LogViewer/WpfDispatcher.cs</files>
  <read_first>
    - LogViewer/LogControlViewModel.cs (lines 570-621 — the three DispatchIfNecessary overloads show exactly how Dispatcher is called today; IDispatcher must mirror these call sites)
  </read_first>
  <action>
    Create LogViewer/IDispatcher.cs:
    - namespace LogViewer
    - public interface IDispatcher with four members:
      1. bool CheckAccess();
      2. T Invoke&lt;T&gt;(Func&lt;T&gt; callback);
      3. Task InvokeAsync(Action callback);
      4. Task&lt;T&gt; InvokeAsync&lt;T&gt;(Func&lt;T&gt; callback);
    - XML doc comments on all members; class-level doc: "Abstracts System.Windows.Threading.Dispatcher for testability."
    - using System.Threading.Tasks; (needed for Task return types)
    - No WPF references.

    Create LogViewer/WpfDispatcher.cs:
    - namespace LogViewer
    - public sealed class WpfDispatcher : IDispatcher
    - using System.Windows.Threading; (acceptable — this is the WPF adapter)
    - Single private readonly field: Dispatcher _dispatcher;
    - Constructor: public WpfDispatcher(Dispatcher dispatcher) — call ArgumentNullException.ThrowIfNull(dispatcher); then assign _dispatcher = dispatcher;
    - CheckAccess() => _dispatcher.CheckAccess();
    - Invoke&lt;T&gt;(Func&lt;T&gt; callback) => _dispatcher.Invoke(callback);
    - async Task InvokeAsync(Action callback) — await _dispatcher.InvokeAsync(callback);
    - async Task&lt;T&gt; InvokeAsync&lt;T&gt;(Func&lt;T&gt; callback) — return await _dispatcher.InvokeAsync(callback);
    - XML doc comments on all public members.
  </action>
  <verify>
    <automated>cd C:/Users/Shady/source/repos/LogViewer && dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net8.0-windows --no-incremental -warnaserror 2>&amp;1 | tail -20</automated>
  </verify>
  <done>IDispatcher.cs and WpfDispatcher.cs compile cleanly. All four interface members are present. WpfDispatcher delegates each call to the wrapped Dispatcher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update LogControlViewModel (IDispatcher + BUG-02) and wire LogControl</name>
  <files>LogViewer/LogControlViewModel.cs, LogViewer/LogControl.xaml.cs</files>
  <read_first>
    - LogViewer/LogControlViewModel.cs (full file — particularly: field declaration line 26, constructor lines 275-295, OnLogEventAsync lines 315-333, and the three DispatchIfNecessary overloads lines 570-621)
    - LogViewer/LogControl.xaml.cs (constructor lines 49-70 — specifically line 52: `_viewModel = new LogControlViewModel(Dispatcher);`)
    - LogViewer/IDispatcher.cs (just created — read the interface members)
  </read_first>
  <behavior>
    - A test instantiating LogControlViewModel with a fake IDispatcher (that returns true from CheckAccess()) must not throw
    - When IsPaused=true and _pauseBuffer.Count == MaxLogSize, a new OnLogEventAsync call must NOT add to the buffer (count stays at MaxLogSize)
    - When IsPaused=true and _pauseBuffer.Count &lt; MaxLogSize, a new OnLogEventAsync call adds to the buffer (count increments)
  </behavior>
  <action>
    In LogViewer/LogControlViewModel.cs make exactly these changes:

    1. Field declaration (line ~26):
       Change: private readonly Dispatcher _dispatcher;
       To:     private readonly IDispatcher _dispatcher;

    2. Remove the using directive for System.Windows.Threading if it is only used for the Dispatcher field/constructor param. Check whether Dispatcher appears anywhere else in the file before removing — if it does (e.g. DispatcherPriority), keep the using.

    3. Constructor parameter (line ~275):
       Change: public LogControlViewModel(Dispatcher dispatcher, ...)
       To:     public LogControlViewModel(IDispatcher dispatcher, ...)
       The null guard and assignment line stay identical in logic.

    4. XML doc comment on the constructor parameter (line ~272):
       Change: "The WPF dispatcher for UI thread synchronization."
       To:     "The dispatcher abstraction for UI thread synchronization."

    5. DispatchIfNecessary&lt;T&gt; (lines ~570-580) — replace Dispatcher-specific calls:
       _dispatcher.CheckAccess() — already works (same method name on IDispatcher)
       _dispatcher.Invoke(callback) — already works (same method name on IDispatcher)
       No code change needed here beyond the field type change at step 1.

    6. DispatchIfNecessaryAsync(Action) (lines ~589-598) — same: method names match IDispatcher. No changes beyond field type.

    7. DispatchIfNecessaryAsync&lt;T&gt; (lines ~607-620) — same.

    8. BUG-02 fix in OnLogEventAsync (lines ~320-328). Inside the lock (_pauseLock) block, before _pauseBuffer.Add(e), insert the cap check:
       Current code inside the lock:
         if (IsPaused)
         {
             _pauseBuffer.Add(e);
             return;
         }
       Replace with:
         if (IsPaused)
         {
             if (_pauseBuffer.Count >= MaxLogSize) return;
             _pauseBuffer.Add(e);
             return;
         }

    In LogViewer/LogControl.xaml.cs:
    1. In the constructor (line ~52), change:
       _viewModel = new LogControlViewModel(Dispatcher);
       To:
       _viewModel = new LogControlViewModel(new WpfDispatcher(Dispatcher));
    2. Ensure `using LogViewer;` is present (it already is, via namespace). WpfDispatcher is in the LogViewer namespace so no additional using is needed.

    Do NOT modify any other logic, fields, properties, or methods. All changes are strictly mechanical type/call substitutions plus the BUG-02 guard.
  </action>
  <verify>
    <automated>cd C:/Users/Shady/source/repos/LogViewer && dotnet test LogViewer.Tests/ --no-build -warnaserror 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - LogControlViewModel field _dispatcher is typed as IDispatcher (grep confirms: "private readonly IDispatcher _dispatcher")
    - Constructor parameter is IDispatcher (grep confirms: "public LogControlViewModel(IDispatcher dispatcher")
    - OnLogEventAsync contains the cap guard (grep confirms: "_pauseBuffer.Count >= MaxLogSize")
    - LogControl.xaml.cs contains "new WpfDispatcher(Dispatcher)"
    - dotnet build LogViewer.sln -warnaserror succeeds for both TFMs
    - dotnet test LogViewer.Tests/ passes all 114 existing tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDispatcher implementations ↔ ViewModel | ViewModel trusts IDispatcher to marshal calls; a malicious mock cannot cause production harm |
| _pauseBuffer ↔ OnLogEventAsync | High-throughput callers on background threads; lock protects Count check and Add atomically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | _pauseBuffer unbounded growth | mitigate | BUG-02: cap check `_pauseBuffer.Count >= MaxLogSize` inside _pauseLock before Add; excess entries are dropped |
| T-02-03 | Tampering | IDispatcher mock in tests | accept | Interface exists only for testability; production code always uses WpfDispatcher; no security boundary crossed |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; pure source changes |
</threat_model>

<verification>
After both tasks:

  cd C:/Users/Shady/source/repos/LogViewer

  # Confirm type changes
  grep -n "IDispatcher _dispatcher" LogViewer/LogControlViewModel.cs
  grep -n "IDispatcher dispatcher" LogViewer/LogControlViewModel.cs
  grep -n "_pauseBuffer.Count >= MaxLogSize" LogViewer/LogControlViewModel.cs
  grep -n "new WpfDispatcher" LogViewer/LogControl.xaml.cs

  # Full build + test
  dotnet build LogViewer.sln -warnaserror 2>&1 | tail -20
  dotnet test LogViewer.Tests/ 2>&1 | tail -20

Expected: All greps find matches. Build succeeds for net8.0-windows and net10.0-windows. All 114 tests pass.
</verification>

<success_criteria>
- IDispatcher.cs: interface with CheckAccess, Invoke&lt;T&gt;, InvokeAsync(Action), InvokeAsync&lt;T&gt;
- WpfDispatcher.cs: sealed class wrapping System.Windows.Threading.Dispatcher, ArgumentNullException guard in constructor
- LogControlViewModel: field and constructor param typed as IDispatcher; _pauseBuffer.Count >= MaxLogSize guard present in OnLogEventAsync
- LogControl.xaml.cs: constructor passes `new WpfDispatcher(Dispatcher)` to LogControlViewModel
- dotnet build -warnaserror: 0 errors, 0 warnings
- dotnet test: 114/114 pass
</success_criteria>

<output>
Create `.planning/phases/02-architecture-foundations/02-02-SUMMARY.md` when done.
</output>
