---
phase: 02-architecture-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - LogViewer/ILoggable.cs
  - LogViewer/LogEventArgs.cs
  - LogViewer/BaseLogger.cs
  - LogViewer/Converters/ColorToBrushConverter.cs
  - LogViewer/Converters/LogLevelColorConverter.cs
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "ILoggable.LogColor is typed as LogColor (not System.Windows.Media.Color); ILoggable.cs has no using System.Windows.Media"
    - "LogEventArgs constructor parameter color is typed as LogColor; LogEventArgs.LogColor property is typed as LogColor"
    - "LogEventArgs.FormatLogMessage {color} placeholder calls LogColor.ToString() (no CultureInfo argument — LogColor.ToString() has no overload)"
    - "BaseLogger fields and constructor parameters for color are typed as LogColor; Colors.Black replaced with LogColor.Black"
    - "ColorToBrushConverter matches on LogColor (not System.Windows.Media.Color) and calls logColor.ToSolidColorBrush()"
    - "LogLevelColorConverter builds its SolidColorBrush using LogColor.FromRgb values passed to ToSolidColorBrush()"
    - "The full solution builds with -warnaserror for both TFMs; all existing tests pass"
  artifacts:
    - path: "LogViewer/ILoggable.cs"
      provides: "Platform-neutral interface with LogColor"
      contains: "LogColor LogColor"
    - path: "LogViewer/LogEventArgs.cs"
      provides: "Event args using LogColor"
      contains: "LogColor color"
    - path: "LogViewer/BaseLogger.cs"
      provides: "Logger implementation using LogColor"
      contains: "LogColor LogColor"
    - path: "LogViewer/Converters/ColorToBrushConverter.cs"
      provides: "Updated converter: LogColor → SolidColorBrush via extension"
      contains: "ToSolidColorBrush"
    - path: "LogViewer/Converters/LogLevelColorConverter.cs"
      provides: "Updated converter: LogLevel → SolidColorBrush via LogColor.FromRgb"
      contains: "LogColor.FromRgb"
  key_links:
    - from: "LogViewer/Converters/ColorToBrushConverter.cs"
      to: "LogViewer/LogColorWpfExtensions.cs"
      via: "logColor.ToSolidColorBrush()"
      pattern: "ToSolidColorBrush"
    - from: "LogViewer/BaseLogger.cs"
      to: "LogViewer/LogColor.cs"
      via: "LogColor.Black default"
      pattern: "LogColor\\.Black"
---

<objective>
Swap `System.Windows.Media.Color` for `LogColor` across the five files that currently reference the WPF type. Plan 01 must complete first (LogColor and LogColorWpfExtensions must be compiled). After this plan, the core interfaces and event args have no WPF dependency, and the XAML converters use the new extension method.

Purpose: Complete ARCH-01 (remove WPF Color from ILoggable/LogEventArgs) and ARCH-02 (converters use ToSolidColorBrush extension). Together with Plan 01, this closes all Phase 2 color-related requirements.

Output: Five modified files; solution compiles; tests pass.
</objective>

<execution_context>
@C:/Users/Shady/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Shady/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Shady/source/repos/LogViewer/.planning/PROJECT.md
@C:/Users/Shady/source/repos/LogViewer/.planning/ROADMAP.md
@C:/Users/Shady/source/repos/LogViewer/.planning/STATE.md
@C:/Users/Shady/source/repos/LogViewer/.planning/phases/02-architecture-foundations/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts created by Plan 01 — executor must NOT re-read LogColor.cs; use these signatures directly -->

From LogViewer/LogColor.cs (created by Plan 01):
  public readonly struct LogColor : IEquatable&lt;LogColor&gt;
    public byte A, R, G, B;
    public static LogColor FromArgb(byte a, byte r, byte g, byte b)
    public static LogColor FromRgb(byte r, byte g, byte b)
    public static readonly LogColor Black  // = FromArgb(255, 0, 0, 0)
    public override string ToString()      // returns "#AARRGGBB"

From LogViewer/LogColorWpfExtensions.cs (created by Plan 01):
  public static class LogColorWpfExtensions
    public static SolidColorBrush ToSolidColorBrush(this LogColor logColor)

<!-- Current signatures being changed -->

ILoggable.cs current:
  using System.Windows.Media;
  Color LogColor { get; set; }    // line 26

LogEventArgs.cs current:
  primary constructor: (LogLevel level, string logHandle, string message, Color color)
  public Color LogColor { get; } = color;
  FormatLogMessage: "color" => LogColor.ToString(CultureInfo.InvariantCulture)   // CultureInfo arg must be REMOVED — LogColor.ToString() has no overload

BaseLogger.cs current:
  using System.Windows.Media;
  protected BaseLogger(string? handle = null, Color? color = null, ...) — line 102
    LogColor = color ?? Colors.Black;
  internal BaseLogger(string categoryName, Color color, ...) — line 128
    LogColor = color;
  public Color LogColor { get; set; }   // line 158

ColorToBrushConverter.cs current:
  if (value is System.Windows.Media.Color color) { return new SolidColorBrush(color); }

LogLevelColorConverter.cs current:
  System.Windows.Media.Color color = System.Windows.Media.Colors.Black;
  color = logLevel switch { ... => System.Windows.Media.Colors.Gray, ... };
  return new SolidColorBrush(color);

LogColor equivalents for LogLevelColorConverter (ARGB values matching System.Windows.Media.Colors.*):
  Gray    → LogColor.FromRgb(128, 128, 128)
  Blue    → LogColor.FromRgb(0, 0, 255)
  Orange  → LogColor.FromRgb(255, 165, 0)
  Red     → LogColor.FromRgb(255, 0, 0)
  DarkRed → LogColor.FromRgb(139, 0, 0)
  Black   → LogColor.Black
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update ILoggable, LogEventArgs, and BaseLogger to use LogColor</name>
  <files>LogViewer/ILoggable.cs, LogViewer/LogEventArgs.cs, LogViewer/BaseLogger.cs</files>
  <read_first>
    - LogViewer/ILoggable.cs (full file — only one line changes: the LogColor property type on line 26)
    - LogViewer/LogEventArgs.cs (full file — primary constructor param type, property type, and FormatLogMessage's CultureInfo.InvariantCulture argument on the "color" case)
    - LogViewer/BaseLogger.cs (full file — two constructor signatures, one field declaration, and Colors.Black usages)
  </read_first>
  <action>
    ILoggable.cs:
    1. Remove: using System.Windows.Media;
    2. Change property declaration: Color LogColor { get; set; }  →  LogColor LogColor { get; set; }
    3. Update the XML doc comment on LogColor from "Gets or sets the color" to "Gets or sets the platform-neutral color associated with this logger instance."
    4. No other changes.

    LogEventArgs.cs:
    1. Remove: using System.Windows.Media;
    2. Primary constructor parameter: change Color color  →  LogColor color
    3. Property: change public Color LogColor  →  public LogColor LogColor
    4. In FormatLogMessage, the "color" switch arm currently reads:
         "color" => LogColor.ToString(CultureInfo.InvariantCulture),
       Change to:
         "color" => LogColor.ToString(),
       (LogColor.ToString() has no CultureInfo overload — the output is the hex string "#AARRGGBB")
    5. The `using System.Globalization;` directive: check whether CultureInfo is still used elsewhere in the file. It is used in LogDateTimeFormatted (LogDateTime.ToString(BaseLogger.LogDateTimeFormat, CultureInfo.InvariantCulture)) and ToString() override — KEEP the using.
    6. Remove: using Newtonsoft.Json; — wait, do NOT remove this unless it is the only change needed for this plan. Actually, Newtonsoft.Json removal is Phase 4 (PKG-01). Keep it.
    7. No other changes.

    BaseLogger.cs:
    1. Remove: using System.Windows.Media;
    2. Protected constructor (line ~102): change Color? color = null  →  LogColor? color = null
       Change: LogColor = color ?? Colors.Black;  →  LogColor = color ?? LogColor.Black;
    3. Internal constructor (line ~128): change Color color  →  LogColor color
       The assignment LogColor = color; stays identical.
    4. Field declaration (line ~158): change public Color LogColor { get; set; }  →  public LogColor LogColor { get; set; }
    5. Update XML doc on the LogColor property and both constructors' color parameters to remove Color references.
    6. Verify no other references to System.Windows.Media or Colors.* remain in BaseLogger.cs after these changes. The using for System.Windows.Media should be safe to remove if it was only used for Color and Colors — check the file for any remaining WPF references before removing.
    7. No other changes.

    After all three edits, run the build. If there are compile errors, read the error output carefully; the most likely issues are:
    - Remaining Colors.* usage → replace with LogColor.FromRgb or LogColor.Black
    - CultureInfo overload on LogColor.ToString() → ensure it was removed
    - Call sites in tests that pass System.Windows.Media.Color → look at the test file, but do NOT modify tests in this plan (check if the tests use Color directly or only through public API)
  </action>
  <verify>
    <automated>cd C:/Users/Shady/source/repos/LogViewer && dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net8.0-windows --no-incremental -warnaserror 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - ILoggable.cs has no System.Windows.Media using; LogColor property typed as LogColor
    - LogEventArgs.cs constructor takes LogColor color; LogColor property is LogColor; FormatLogMessage uses LogColor.ToString() with no CultureInfo argument
    - BaseLogger.cs has no System.Windows.Media using; Color? → LogColor?; Colors.Black → LogColor.Black; public field is LogColor
    - dotnet build succeeds with -warnaserror
  </done>
</task>

<task type="auto">
  <name>Task 2: Update XAML converters to use LogColor and ToSolidColorBrush</name>
  <files>LogViewer/Converters/ColorToBrushConverter.cs, LogViewer/Converters/LogLevelColorConverter.cs</files>
  <read_first>
    - LogViewer/Converters/ColorToBrushConverter.cs (full file — 54 lines; change the type pattern match)
    - LogViewer/Converters/LogLevelColorConverter.cs (full file — 63 lines; change the color mapping)
  </read_first>
  <action>
    ColorToBrushConverter.cs:
    1. Add: using LogViewer; (to access LogColor and the ToSolidColorBrush extension)
    2. In the Convert method, change:
         if (value is System.Windows.Media.Color color)
         {
             return new System.Windows.Media.SolidColorBrush(color);
         }
         return System.Windows.Media.Brushes.Black;
       To:
         if (value is LogColor logColor)
         {
             return logColor.ToSolidColorBrush();
         }
         return LogColor.Black.ToSolidColorBrush();
    3. Update the XML doc on the Convert method to say it accepts a LogColor (not System.Windows.Media.Color).
    4. Keep System.Windows.Media in scope (for SolidColorBrush return type used in XML docs and ConvertBack's Binding.DoNothing). The using for System.Windows.Data is already present for IValueConverter.
    5. No other changes.

    LogLevelColorConverter.cs:
    1. Add: using LogViewer; (for LogColor and ToSolidColorBrush)
    2. Replace the entire Convert method body. Current body uses a local System.Windows.Media.Color variable and a switch that assigns Colors.* values. New body:
         if (value is LogLevel logLevel)
         {
             LogColor logColor = logLevel switch
             {
                 LogLevel.Trace    => LogColor.FromRgb(128, 128, 128),
                 LogLevel.Debug    => LogColor.FromRgb(0, 0, 255),
                 LogLevel.Warning  => LogColor.FromRgb(255, 165, 0),
                 LogLevel.Error    => LogColor.FromRgb(255, 0, 0),
                 LogLevel.Critical => LogColor.FromRgb(139, 0, 0),
                 _                 => LogColor.Black
             };
             return logColor.ToSolidColorBrush();
         }
         return LogColor.Black.ToSolidColorBrush();
    3. Update XML doc on Convert to reflect it now uses LogColor internally.
    4. Remove the local variable `System.Windows.Media.Color color = System.Windows.Media.Colors.Black;` that previously prefaced the switch — it is no longer needed.
    5. No other changes to the file.

    Verify the ARGB values against the System.Windows.Media.Colors documentation:
    - Colors.Gray    = #FF808080 → R=128, G=128, B=128 ✓
    - Colors.Blue    = #FF0000FF → R=0, G=0, B=255 ✓
    - Colors.Orange  = #FFFFA500 → R=255, G=165, B=0 ✓
    - Colors.Red     = #FFFF0000 → R=255, G=0, B=0 ✓
    - Colors.DarkRed = #FF8B0000 → R=139, G=0, B=0 ✓
  </action>
  <verify>
    <automated>cd C:/Users/Shady/source/repos/LogViewer && dotnet build LogViewer.sln --no-incremental -warnaserror 2>&amp;1 | tail -30 && dotnet test LogViewer.Tests/ 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - ColorToBrushConverter matches on LogColor and calls .ToSolidColorBrush()
    - LogLevelColorConverter uses LogColor.FromRgb switch and .ToSolidColorBrush()
    - No reference to System.Windows.Media.Colors in either converter
    - Full solution build (both TFMs) succeeds with -warnaserror
    - All 114 existing tests pass
    - grep -rn "System.Windows.Media.Color" LogViewer/ILoggable.cs LogViewer/LogEventArgs.cs LogViewer/BaseLogger.cs returns no matches
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LogEventArgs.LogColor ↔ XAML converters | Color data flows from loggers through event args to UI converters; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | LogColor.ToString() in FormatLogMessage | accept | Color hex strings carry no sensitive data; format is #AARRGGBB |
| T-02-05 | Tampering | LogColor.FromRgb hardcoded values in LogLevelColorConverter | accept | Values are compile-time constants matching WPF Colors.* exactly; no runtime input |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; pure source changes |
</threat_model>

<verification>
Final check after both tasks:

  cd C:/Users/Shady/source/repos/LogViewer

  # Confirm no WPF Color type remains in core interface files
  grep -n "System.Windows.Media.Color\|System.Windows.Media.Colors" LogViewer/ILoggable.cs LogViewer/LogEventArgs.cs LogViewer/BaseLogger.cs

  # Confirm converters use the extension
  grep -n "ToSolidColorBrush" LogViewer/Converters/ColorToBrushConverter.cs LogViewer/Converters/LogLevelColorConverter.cs

  # Full build and test
  dotnet build LogViewer.sln -warnaserror 2>&1 | tail -20
  dotnet test LogViewer.Tests/ 2>&1 | tail -20

Expected: grep for System.Windows.Media.Color returns zero matches in the three core files. Both converters contain ToSolidColorBrush. Build succeeds. 114 tests pass.
</verification>

<success_criteria>
- ILoggable, LogEventArgs, BaseLogger: no System.Windows.Media using; all Color references replaced with LogColor
- LogEventArgs.FormatLogMessage "color" arm: LogColor.ToString() with no CultureInfo argument
- BaseLogger protected constructor default: LogColor.Black (not Colors.Black)
- ColorToBrushConverter: pattern-matches LogColor, calls .ToSolidColorBrush()
- LogLevelColorConverter: switch returns LogColor.FromRgb values, calls .ToSolidColorBrush()
- dotnet build LogViewer.sln -warnaserror: 0 errors, 0 warnings, both TFMs
- dotnet test: 114/114 pass
</success_criteria>

<output>
Create `.planning/phases/02-architecture-foundations/02-03-SUMMARY.md` when done.
</output>
