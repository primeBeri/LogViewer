---
phase: 02-architecture-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LogViewer/LogColor.cs
  - LogViewer/LogColorWpfExtensions.cs
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "LogColor is a readonly struct with byte A, R, G, B fields in the LogViewer namespace"
    - "LogColor has static factory methods: FromArgb(byte a, byte r, byte g, byte b), FromRgb(byte r, byte g, byte b), and a Black static property"
    - "LogColor.ToString() returns a human-readable ARGB string (e.g. '#FF000000') for use in FormatLogMessage's {color} placeholder"
    - "LogColorWpfExtensions.ToSolidColorBrush() converts a LogColor to a System.Windows.Media.SolidColorBrush"
    - "The project compiles (dotnet build) with both new files present"
  artifacts:
    - path: "LogViewer/LogColor.cs"
      provides: "Platform-neutral color value type"
      contains: "public readonly struct LogColor"
    - path: "LogViewer/LogColorWpfExtensions.cs"
      provides: "WPF-specific extension to convert LogColor to SolidColorBrush"
      contains: "ToSolidColorBrush"
  key_links:
    - from: "LogViewer/LogColorWpfExtensions.cs"
      to: "LogViewer/LogColor.cs"
      via: "extension method on LogColor"
      pattern: "this LogColor"
---

<objective>
Create the two new files that form the platform-neutral color abstraction: `LogColor` (a pure C# struct with no WPF reference) and `LogColorWpfExtensions` (a WPF-only extension class that converts `LogColor` to `SolidColorBrush`). These are the foundation that Plans 02 and 03 build on. No existing files are modified in this plan.

Purpose: Establish the type contracts before modifying callers. Wave 2 (Plan 02) will swap `System.Windows.Media.Color` for `LogColor` across `ILoggable`, `LogEventArgs`, and `BaseLogger` — it must have `LogColor` already compiled and available.

Output: Two new source files; project must compile cleanly after this plan.
</objective>

<execution_context>
@C:/Users/Shady/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Shady/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Shady/source/repos/LogViewer/.planning/PROJECT.md
@C:/Users/Shady/source/repos/LogViewer/.planning/ROADMAP.md
@C:/Users/Shady/source/repos/LogViewer/.planning/STATE.md
</context>

<interfaces>
<!-- New types being created in this plan. Wave 2 executors read these for contract reference. -->

LogViewer/LogColor.cs — target shape:
  namespace LogViewer
  public readonly struct LogColor : IEquatable<LogColor>
    Fields: byte A, R, G, B (public)
    Static properties: Black (A=255,R=0,G=0,B=0)
    Factory: static LogColor FromArgb(byte a, byte r, byte g, byte b)
    Factory: static LogColor FromRgb(byte r, byte g, byte b)  // A defaults to 255
    ToString(): returns "#AARRGGBB" hex string
    Equals / GetHashCode / == / != operators

LogViewer/LogColorWpfExtensions.cs — target shape:
  namespace LogViewer
  public static class LogColorWpfExtensions
    public static SolidColorBrush ToSolidColorBrush(this LogColor logColor)
      => new SolidColorBrush(System.Windows.Media.Color.FromArgb(logColor.A, logColor.R, logColor.G, logColor.B))
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LogColor struct</name>
  <files>LogViewer/LogColor.cs</files>
  <read_first>
    - LogViewer/ILoggable.cs (see current Color property declaration — this replaces it)
    - LogViewer/LogEventArgs.cs (see Color constructor param and LogColor.ToString usage in FormatLogMessage)
    - LogViewer/BaseLogger.cs (see Color? color = null defaults and Colors.Black usage)
  </read_first>
  <behavior>
    - LogColor.FromArgb(255, 0, 0, 0) returns a struct where A=255, R=0, G=0, B=0
    - LogColor.FromRgb(255, 0, 0) returns a struct where A=255, R=255, G=0, B=0
    - LogColor.Black is equivalent to LogColor.FromArgb(255, 0, 0, 0)
    - LogColor.Black.ToString() returns "#FF000000"
    - LogColor.FromArgb(255, 128, 64, 32).ToString() returns "#FF804020"
    - Two LogColors with identical ARGB values are equal (Equals, ==)
    - Two LogColors with different ARGB values are not equal
  </behavior>
  <action>
    Create LogViewer/LogColor.cs in the LogViewer namespace. The struct must be:
    - public readonly struct LogColor : IEquatable&lt;LogColor&gt;
    - Four public byte fields: A, R, G, B (in that declaration order)
    - Private constructor: LogColor(byte a, byte r, byte g, byte b) — assigns all four fields
    - public static LogColor FromArgb(byte a, byte r, byte g, byte b) — calls the private constructor
    - public static LogColor FromRgb(byte r, byte g, byte b) — calls FromArgb with a=255
    - public static readonly LogColor Black = FromArgb(255, 0, 0, 0)
    - public override string ToString() — returns string formatted as "#AARRGGBB" using two-digit uppercase hex per channel: $"#{A:X2}{R:X2}{G:X2}{B:X2}"
    - Implement IEquatable&lt;LogColor&gt;: Equals(LogColor other) compares all four fields
    - Override Equals(object?) and GetHashCode() (use HashCode.Combine(A, R, G, B))
    - == and != operators
    - XML doc comments on all public members (required by project standards — GenerateDocumentationFile=True)
    - No using directives needed beyond System (for HashCode)
    - Do NOT reference System.Windows or any WPF assembly
  </action>
  <verify>
    <automated>cd C:/Users/Shady/source/repos/LogViewer && dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net8.0-windows --no-incremental -warnaserror 2>&amp;1 | tail -20</automated>
  </verify>
  <done>LogColor.cs compiles cleanly. dotnet build reports 0 errors. The struct has A, R, G, B fields, FromArgb, FromRgb, Black, and a ToString() returning "#AARRGGBB" hex format.</done>
</task>

<task type="auto">
  <name>Task 2: Create LogColorWpfExtensions</name>
  <files>LogViewer/LogColorWpfExtensions.cs</files>
  <read_first>
    - LogViewer/LogColor.cs (just created — read the A, R, G, B field names)
    - LogViewer/Converters/ColorToBrushConverter.cs (shows current pattern: new SolidColorBrush(color))
  </read_first>
  <action>
    Create LogViewer/LogColorWpfExtensions.cs in the LogViewer namespace. Requirements:

    - public static class LogColorWpfExtensions
    - Single extension method: public static System.Windows.Media.SolidColorBrush ToSolidColorBrush(this LogColor logColor)
    - Implementation: return new System.Windows.Media.SolidColorBrush(System.Windows.Media.Color.FromArgb(logColor.A, logColor.R, logColor.G, logColor.B));
    - using directives: System.Windows.Media (acceptable here — this is the WPF extension file; the WPF reference is intentional)
    - XML doc comment on the class and method explaining: "WPF-specific extensions for LogColor. Not for use outside WPF assemblies."
    - No other methods — this class has exactly one responsibility.
  </action>
  <verify>
    <automated>cd C:/Users/Shady/source/repos/LogViewer && dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net8.0-windows --no-incremental -warnaserror 2>&amp;1 | tail -20</automated>
  </verify>
  <done>LogColorWpfExtensions.cs compiles. dotnet build reports 0 errors and 0 warnings for both new files. ToSolidColorBrush() is present as an extension method on LogColor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LogColor struct ↔ callers | LogColor is a value type passed by value; no reference aliasing risks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | LogColor struct fields | accept | Fields are public readonly bytes — immutable value type; no mutation surface |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan; pure source files only |
</threat_model>

<verification>
After both tasks complete, run the full solution build to confirm no regressions:

  cd C:/Users/Shady/source/repos/LogViewer
  dotnet build LogViewer.sln --no-incremental -warnaserror 2>&1 | tail -30

Expected: Build succeeds for both net8.0-windows and net10.0-windows TFMs. Zero errors. LogColor.cs and LogColorWpfExtensions.cs appear in the build output.
</verification>

<success_criteria>
- LogColor.cs exists with: public readonly struct LogColor, byte A/R/G/B fields, FromArgb, FromRgb, Black static, ToString() as "#AARRGGBB", IEquatable, == / !=
- LogColorWpfExtensions.cs exists with: static class, ToSolidColorBrush(this LogColor) extension method
- No reference to System.Windows or WPF in LogColor.cs
- dotnet build LogViewer.sln succeeds for both TFMs with -warnaserror
- All public members have XML doc comments
</success_criteria>

<output>
Create `.planning/phases/02-architecture-foundations/02-01-SUMMARY.md` when done.
</output>
