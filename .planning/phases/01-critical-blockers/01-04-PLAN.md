---
phase: 1
plan: 01-04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - TFM-03
must_haves:
  truths:
    - "README and NuGet package metadata accurately document both net8.0-windows and net10.0-windows as supported frameworks"
  artifacts:
    - path: "README.md"
      provides: "Updated framework documentation"
      contains: "net8.0-windows"
  key_links:
    - from: "README.md"
      to: "NuGet consumers"
      via: "Header badge and Installation section listing both TFMs"
      pattern: "net8\\.0-windows.*net10\\.0-windows|net10\\.0-windows.*net8\\.0-windows"
---

<objective>
Update `README.md` to replace all .NET 8-only framework mentions with accurate documentation that lists both `net8.0-windows` and `net10.0-windows` as supported targets.

Purpose: The README currently states "Framework: .NET 8.0 Windows" and describes the library as targeting ".NET 8 WPF applications" only. After TFM-01 ships the multi-target csproj, the documentation must match the package capabilities so NuGet consumers know both runtimes are supported.
Output: Updated README.md with corrected header badge, introduction paragraph, and installation section.
</objective>

<execution_context>
@C:\Users\Shady\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Shady\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key lines in README.md that require updating — extracted from README.md -->

Line 3 (header badge — current):
```
**Version:** 0.3.1 | **Framework:** .NET 8.0 Windows | **License:** MIT
```

Line 5 (introduction paragraph — current):
```
A real-time log viewer control for .NET 8 WPF applications. LogViewer integrates seamlessly with `Microsoft.Extensions.Logging` to display structured, color-coded, filterable logs in your application's UI.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update README framework mentions to reflect dual-TFM support</name>

  <read_first>README.md</read_first>

  <files>README.md</files>

  <action>
Make the following targeted text replacements in README.md:

**Change 1 — Header badge (line 3):**

Replace:
```
**Version:** 0.3.1 | **Framework:** .NET 8.0 Windows | **License:** MIT
```
With:
```
**Version:** 0.3.1 | **Frameworks:** .NET 8+ Windows (`net8.0-windows`, `net10.0-windows`) | **License:** MIT
```

**Change 2 — Introduction paragraph (line 5):**

Replace:
```
A real-time log viewer control for .NET 8 WPF applications. LogViewer integrates seamlessly with `Microsoft.Extensions.Logging` to display structured, color-coded, filterable logs in your application's UI.
```
With:
```
A real-time log viewer control for .NET 8 and .NET 10 WPF applications. LogViewer integrates seamlessly with `Microsoft.Extensions.Logging` to display structured, color-coded, filterable logs in your application's UI.
```

**Change 3 — Installation section (NuGet Package subsection):**

Locate the `### NuGet Package` subsection. After the `dotnet add package` code block and before the `Or add to your .csproj:` line, insert the following note on a new line:

```
> **Supported frameworks:** `net8.0-windows`, `net10.0-windows`
```

The resulting Installation block should look like:

```markdown
### NuGet Package

```bash
dotnet add package RealTimeLogStream --source https://nuget.pkg.github.com/ArisenVendetta/index.json
```

> **Supported frameworks:** `net8.0-windows`, `net10.0-windows`

Or add to your `.csproj`:
```

Do not modify any other section of the README.md.
  </action>

  <verify>
    <automated>Select-String -Path "README.md" -Pattern "net8\.0-windows.*net10\.0-windows|net10\.0-windows.*net8\.0-windows" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>

  <acceptance_criteria>
1. `Select-String -Path "README.md" -Pattern "\.NET 8\.0 Windows"` returns no matches (old singular badge text is gone)
2. `Select-String -Path "README.md" -Pattern "Frameworks.*net8\.0-windows"` returns at least one match (header badge updated)
3. `Select-String -Path "README.md" -Pattern "net10\.0-windows"` returns at least two matches (badge and installation note)
4. `Select-String -Path "README.md" -Pattern "\.NET 8 WPF applications"` returns no matches (introduction paragraph updated)
5. `Select-String -Path "README.md" -Pattern "\.NET 8 and \.NET 10 WPF applications"` returns exactly one match
6. `Select-String -Path "README.md" -Pattern "Supported frameworks"` returns at least one match (installation note added)
7. The "Version: 0.3.1" text in the header badge is unchanged
  </acceptance_criteria>

  <done>README header badge shows both net8.0-windows and net10.0-windows; introduction paragraph says ".NET 8 and .NET 10"; installation section notes supported frameworks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → NuGet.org / GitHub Packages listing | Documentation is rendered publicly; incorrect claims mislead consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | README.md content | accept | README is source-controlled; changes are reviewed via PR. No runtime security impact. |
| T-01-09 | Repudiation | Documented capabilities vs actual package | mitigate | This plan is executed after 01-01 (csproj multi-target) ships, ensuring documentation matches actual package output. Do not merge this README change independently of the csproj change. |
</threat_model>

<verification>
1. `Select-String -Path "README.md" -Pattern "\.NET 8\.0 Windows"` returns 0 matches
2. `Select-String -Path "README.md" -Pattern "net8\.0-windows"` returns at least 2 matches
3. `Select-String -Path "README.md" -Pattern "net10\.0-windows"` returns at least 2 matches
4. README renders correctly in a Markdown previewer (no broken formatting introduced)
</verification>

<success_criteria>
- Header badge reads: `**Frameworks:** .NET 8+ Windows (net8.0-windows, net10.0-windows)`
- Introduction paragraph references ".NET 8 and .NET 10 WPF applications"
- Installation section includes a `> **Supported frameworks:**` note listing both TFMs
- No other README content is changed
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-blockers/01-04-SUMMARY.md`
</output>
