---
phase: 03-global-state-elimination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LogViewer/LogViewerOptions.cs
  - LogViewer/IBaseLoggerSink.cs
  - LogViewer/BaseLoggerSink.cs
  - LogViewer/BaseLoggerLoggingBuilderExtensions.cs
  - LogViewer/BaseLoggerProviderOptions.cs
  - LogViewer/LogEventArgs.cs
  - LogViewer/LogControlViewModel.cs
  - LogViewer/BaseLogger.Settings.cs
  - LogViewer.Tests/TestBaseLoggerSink.cs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04

must_haves:
  truths:
    - "LogViewerOptions consolidates MaxLogQueueSize, LogDateTimeFormat, LogUTCTime, ExcludeCharsFromHandle, LogExportFormat, CategoryColors, MinimumLevel, StripNamespaceFromCategory"
    - "AddLogViewer() registers IOptions<LogViewerOptions> so consumers can configure via appsettings.json or Configure<LogViewerOptions>()"
    - "AddLogViewerCore no longer writes BaseLogger.LogDateTimeFormat or BaseLogger.LogUTCTime — the DI path reads from sink.Options"
    - "LogEventArgs.LogDateTimeFormatted and FormatLogMessage fall back to BaseLogger statics when sink.Options is null (inheritance pattern remains usable)"
    - "LogControlViewModel.MaxLogSize default reads from _sink.Options?.MaxLogQueueSize with BaseLogger.MaxLogQueueSize fallback"
    - "BaseLogger.Initialize(), Shutdown(), CreateLogger(), CreateLogger<T>() all carry [Obsolete] with CS0618 warning message"
    - "All existing 125 tests pass after the changes"
  artifacts:
    - path: "LogViewer/LogViewerOptions.cs"
      provides: "New canonical options POCO registered as IOptions<LogViewerOptions>"
      exports: ["LogViewerOptions"]
    - path: "LogViewer/IBaseLoggerSink.cs"
      provides: "Options property on the sink interface"
      contains: "LogViewerOptions? Options"
    - path: "LogViewer/BaseLoggerSink.cs"
      provides: "Options property implementation"
      contains: "public LogViewerOptions? Options"
    - path: "LogViewer/BaseLoggerLoggingBuilderExtensions.cs"
      provides: "Registers IOptions<LogViewerOptions>; removes static mutation"
      contains: "services.Configure<LogViewerOptions>"
    - path: "LogViewer/BaseLogger.Settings.cs"
      provides: "Obsolete markers on inheritance-pattern entry points"
      contains: "[Obsolete"
  key_links:
    - from: "LogViewer/BaseLoggerLoggingBuilderExtensions.cs"
      to: "LogViewer/IBaseLoggerSink.cs"
      via: "sink.Options = opts assignment in AddLogViewerCore"
      pattern: "sink\\.Options\\s*="
    - from: "LogViewer/LogEventArgs.cs"
      to: "LogViewer/BaseLoggerSink.cs"
      via: "BaseLoggerSink.Instance.Options?.LogDateTimeFormat fallback read"
      pattern: "BaseLoggerSink\\.Instance\\.Options"
    - from: "LogViewer/LogControlViewModel.cs"
      to: "LogViewer/IBaseLoggerSink.cs"
      via: "_sink.Options?.MaxLogQueueSize in MaxLogSize default initializer"
      pattern: "_sink\\.Options"
---

<objective>
Create LogViewerOptions, wire it into IBaseLoggerSink and AddLogViewerCore, remove static configuration mutations from the DI path, and mark the inheritance-pattern entry points obsolete.

Purpose: STATE-01 through STATE-04 — all shared library configuration in the DI code path flows through IOptions<LogViewerOptions> rather than global statics. The inheritance pattern continues to work via static-fallback reads.
Output: LogViewerOptions.cs (new), updated IBaseLoggerSink/BaseLoggerSink, updated AddLogViewer extensions, updated LogEventArgs, updated LogControlViewModel, Obsolete markers on BaseLogger, updated TestBaseLoggerSink.
</objective>

<execution_context>
@C:\Users\Shady\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Shady\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-global-state-elimination/03-CONTEXT.md
</context>

<interfaces>
<!-- Key contracts the executor will create or extend. -->

Current IBaseLoggerSink (LogViewer/IBaseLoggerSink.cs) — ADD Options property:
```csharp
public interface IBaseLoggerSink
{
    event LogEvent? LogReceived;
    void Write(LogEventArgs logEvent);
    int MaxQueueSize { get; set; }
    ConcurrentQueue<LogEventArgs> LogQueue { get; }
    // NEW:
    LogViewerOptions? Options { get; set; }
}
```

Current BaseLoggerProviderOptions properties to migrate into LogViewerOptions:
- MaxQueueSize (int, 10000) → rename to MaxLogQueueSize
- MinimumLevel (LogLevel, Trace)
- CategoryColors (Dictionary<string,LogColor>, OrdinalIgnoreCase)
- DateTimeFormat (string, "yyyy-MM-dd HH:mm:ss.fff (zzz)") → rename to LogDateTimeFormat
- UseUtcTime (bool, false) → rename to LogUTCTime
- StripNamespaceFromCategory (bool, true)
- ADD: ExcludeCharsFromHandle (ICollection<char>, ['.', '-', ' '])
- ADD: LogExportFormat (string, BaseLogger.DefaultLogExportFormat)

AddLogViewerCore current static mutations to REMOVE:
```csharp
// DELETE THESE TWO LINES:
BaseLogger.LogDateTimeFormat = options.DateTimeFormat;
BaseLogger.LogUTCTime = options.UseUtcTime;
```

LogEventArgs static reads to update (with fallback pattern):
- Line 48: LogDateTimeFormatted → BaseLoggerSink.Instance.Options?.LogDateTimeFormat ?? BaseLogger.LogDateTimeFormat
- Line 73: ToString() → same pattern
- Line 95: FormatLogMessage null-format branch → BaseLoggerSink.Instance.Options?.LogExportFormat ?? BaseLogger.LogExportFormat

LogControlViewModel line 237 (MaxLogSize default):
```csharp
// CHANGE FROM:
public int MaxLogSize { get; set; } = BaseLogger.MaxLogQueueSize;
// TO (requires reading from _sink — do in constructor initializer or property backing field):
// Set MaxLogSize = _sink.Options?.MaxLogQueueSize ?? BaseLogger.MaxLogQueueSize; in constructor body
```

Obsolete message to use (exactly):
"Use builder.AddLogViewer() with the DI pattern. See the migration guide in README.md."
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LogViewerOptions and update IBaseLoggerSink + implementations</name>
  <files>LogViewer/LogViewerOptions.cs, LogViewer/IBaseLoggerSink.cs, LogViewer/BaseLoggerSink.cs, LogViewer.Tests/TestBaseLoggerSink.cs</files>
  <action>
Create LogViewer/LogViewerOptions.cs as a public class (not struct — IOptions requires class) in namespace LogViewer. Include XML doc comments on the class and every property (GenerateDocumentationFile=True is enforced). Properties:

- MaxLogQueueSize (int, default 10000) — XML doc: maximum number of log entries to display
- LogDateTimeFormat (string, default "yyyy-MM-dd HH:mm:ss.fff (zzz)") — XML doc: timestamp format string
- LogUTCTime (bool, default false) — XML doc: use UTC timestamps
- ExcludeCharsFromHandle (ICollection<char>, default initialised to ['.', '-', ' ']) — XML doc: characters stripped from logger category names to form the handle
- LogExportFormat (string, default BaseLogger.DefaultLogExportFormat) — XML doc: format string for text export; supports {timestamp}, {loglevel}, {threadid}, {color}, {handle}, {message}
- CategoryColors (Dictionary<string, LogColor>, StringComparer.OrdinalIgnoreCase) — XML doc: per-category display colours
- MinimumLevel (LogLevel, default LogLevel.Trace) — XML doc: minimum severity level passed to LogViewer
- StripNamespaceFromCategory (bool, default true) — XML doc: strip namespace prefix from category name when forming handle

Add LogViewerOptions? Options { get; set; } to IBaseLoggerSink in LogViewer/IBaseLoggerSink.cs. Add the XML doc summary comment: "Gets or sets the active LogViewerOptions injected by AddLogViewer(). Null when using the inheritance pattern; consumers read statics as fallback in that case."

In LogViewer/BaseLoggerSink.cs, add a public auto-property implementing the interface member: public LogViewerOptions? Options { get; set; } — no other changes to the sink.

In LogViewer.Tests/TestBaseLoggerSink.cs, add the property to implement the updated interface: public LogViewerOptions? Options { get; set; } — placed alongside the existing MaxQueueSize property.
  </action>
  <verify>
    <automated>dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net8.0-windows --no-restore --no-incremental 2>&1 | tail -5</automated>
  </verify>
  <done>LogViewerOptions.cs exists with all 8 properties; IBaseLoggerSink, BaseLoggerSink, and TestBaseLoggerSink all compile with the Options property; dotnet build reports 0 errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire LogViewerOptions into AddLogViewer, remove static mutations, mark BaseLogger entry points Obsolete</name>
  <files>LogViewer/BaseLoggerLoggingBuilderExtensions.cs, LogViewer/BaseLoggerProviderOptions.cs, LogViewer/LogEventArgs.cs, LogViewer/LogControlViewModel.cs, LogViewer/BaseLogger.Settings.cs</files>
  <action>
**BaseLoggerLoggingBuilderExtensions.cs:**

Change all four AddLogViewer overloads to accept Action<LogViewerOptions> instead of Action<BaseLoggerProviderOptions>. Update XML docs on each overload accordingly.

Update AddLogViewerCore signature to: private static ILoggingBuilder AddLogViewerCore(ILoggingBuilder builder, ILoggerFactory? innerFactory, Action<LogViewerOptions> configure)

Inside AddLogViewerCore:
1. Instantiate: var options = new LogViewerOptions(); configure(options);
2. DELETE the two static mutation lines: BaseLogger.LogDateTimeFormat = ... and BaseLogger.LogUTCTime = ... — do not leave them in any form
3. Set sink.Options = options; (after var sink = BaseLoggerSink.Instance;)
4. Keep sink.MaxQueueSize = options.MaxLogQueueSize; (renamed from MaxQueueSize)
5. Add options registration for IOptions<LogViewerOptions>: builder.Services.AddOptions(); then builder.Services.Configure<LogViewerOptions>(o => configure(o)); — this allows consumers to also configure via appsettings.json by calling builder.Services.Configure<LogViewerOptions>(configuration.GetSection("LogViewer")) separately
6. Keep provider.MinimumLevel = options.MinimumLevel; and provider.StripNamespaceFromCategory = options.StripNamespaceFromCategory; (updated to read from LogViewerOptions)
7. Keep provider.SetCategoryColors(options.CategoryColors); (property is CategoryColors in LogViewerOptions)

**BaseLoggerProviderOptions.cs:**

Mark the class with [Obsolete("Use LogViewerOptions instead. BaseLoggerProviderOptions will be removed in a future version.", false)] — do NOT delete the file yet (leave it so existing consumer code gets a warning, not a compile error). Add a using directive for System if not already present.

**LogEventArgs.cs:**

Update LogDateTimeFormatted (line 48):
```csharp
[JsonIgnore] public string LogDateTimeFormatted => LogDateTime.ToString(
    BaseLoggerSink.Instance.Options?.LogDateTimeFormat ?? BaseLogger.LogDateTimeFormat,
    CultureInfo.InvariantCulture);
```

Update ToString() body (line 73) — replace BaseLogger.LogDateTimeFormat with: BaseLoggerSink.Instance.Options?.LogDateTimeFormat ?? BaseLogger.LogDateTimeFormat

Update FormatLogMessage null-format branch (line 95) — replace BaseLogger.LogExportFormat with: BaseLoggerSink.Instance.Options?.LogExportFormat ?? BaseLogger.LogExportFormat

**LogControlViewModel.cs:**

Remove the initializer on MaxLogSize (line 237): change public int MaxLogSize { get; set; } = BaseLogger.MaxLogQueueSize; to a backing field + property, or simply remove the initializer and set the value in the constructor body after _sink is assigned:

In the constructor body, after the _sink assignment line, add: MaxLogSize = _sink.Options?.MaxLogQueueSize ?? BaseLogger.MaxLogQueueSize;

If Fody/PropertyChanged requires a public auto-property, keep the [AddINotifyPropertyChangedInterface] attribute on the class and declare: public int MaxLogSize { get; set; } — initialisation moves to the constructor.

**BaseLogger.Settings.cs:**

Add [Obsolete("Use builder.AddLogViewer() with the DI pattern. See the migration guide in README.md.", false)] to:
- Initialize(ILoggerFactory?, int, string, string)
- Shutdown()
- CreateLogger(string?, LogColor?, LogLevel)
- CreateLogger<T>(LogColor?, LogLevel)

Place each [Obsolete] attribute on the line immediately before the public static keyword of each method. Do not add it to any other members (not LoggerFactory, not SanitizeHandle, not the static properties — those remain as normal fallbacks for the inheritance pattern).
  </action>
  <verify>
    <automated>dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net8.0-windows --no-restore --no-incremental 2>&1 | tail -20</automated>
  </verify>
  <done>
- Build reports 0 errors (warnings for CS0618 in LogViewerExample are acceptable — those call Initialize())
- No references to BaseLogger.LogDateTimeFormat = or BaseLogger.LogUTCTime = in BaseLoggerLoggingBuilderExtensions.cs
- sink.Options = options; is present in AddLogViewerCore
- builder.Services.Configure&lt;LogViewerOptions&gt; is present
- [Obsolete] appears on Initialize, Shutdown, CreateLogger, CreateLogger&lt;T&gt; in BaseLogger.Settings.cs
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify full build and test suite</name>
  <files></files>
  <action>
Run the full solution build and test suite. Do not modify any files in this task — this is a verification-only step.

If test failures occur:
- Check if any test references BaseLoggerProviderOptions by name — if so, it will now get a CS0618 warning (acceptable) or the test project may reference an overload signature that changed. Update the test's AddLogViewer call to use Action<LogViewerOptions> if needed.
- Check if LogExporterTests uses [Collection(GlobalStateCollection.Name)] — this collection serialises tests that touch BaseLogger statics. No change needed there.
- Check if any test constructs LogControlViewModel and previously relied on MaxLogSize defaulting to BaseLogger.MaxLogQueueSize at property declaration time vs. constructor time — the default value may now differ if BaseLogger.MaxLogQueueSize was mutated before construction. If a test sets BaseLogger.MaxLogQueueSize = X and expects MaxLogSize == X on a new VM, it will still pass because the constructor reads the static as fallback (sink.Options is null in tests using TestBaseLoggerSink by default).

After any fixes, re-run until: dotnet test LogViewer.Tests/ -- exits 0 with all tests green.
  </action>
  <verify>
    <automated>dotnet build LogViewer.sln -p:TargetFramework=net8.0-windows --no-restore 2>&1 | tail -5 && dotnet test LogViewer.Tests/ --no-build 2>&1 | tail -10</automated>
  </verify>
  <done>dotnet test exits 0; pass count matches or exceeds 125 (the pre-phase count); no test regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer app → AddLogViewer() | Consumer passes Action<LogViewerOptions> delegate; options values are caller-controlled |
| IOptions<LogViewerOptions> → DI container | Options bound from appsettings.json; config values are environment-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | LogViewerOptions.CategoryColors | accept | Color map is display metadata; no PII or secrets |
| T-03-02 | Tampering | BaseLoggerSink.Options (public setter) | accept | Internal library state; no cross-process boundary; setter is intentional for DI wiring |
| T-03-03 | Elevation of Privilege | [Obsolete] markers | accept | CS0618 warnings only (isError=false); no runtime enforcement needed for pre-v1.0 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; pure C# source changes only |
</threat_model>

<verification>
1. `dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net8.0-windows --no-restore --no-incremental` — 0 errors
2. `dotnet build LogViewer/LogViewer.csproj -p:TargetFramework=net10.0-windows --no-restore --no-incremental` — 0 errors
3. `dotnet test LogViewer.Tests/` — all tests pass (≥125)
4. Grep: `grep -n "BaseLogger\.LogDateTimeFormat\s*=" LogViewer/BaseLoggerLoggingBuilderExtensions.cs` — 0 matches (static mutation removed)
5. Grep: `grep -n "BaseLogger\.LogUTCTime\s*=" LogViewer/BaseLoggerLoggingBuilderExtensions.cs` — 0 matches
6. Grep: `grep -n "Obsolete" LogViewer/BaseLogger.Settings.cs` — 4 matches (one per deprecated method)
7. Grep: `grep -n "sink\.Options\s*=" LogViewer/BaseLoggerLoggingBuilderExtensions.cs` — 1 match
8. Grep: `grep -n "Configure<LogViewerOptions>" LogViewer/BaseLoggerLoggingBuilderExtensions.cs` — 1 match
</verification>

<success_criteria>
1. LogViewerOptions.cs exists with all 8 properties and XML docs
2. IBaseLoggerSink, BaseLoggerSink, and TestBaseLoggerSink all implement Options
3. AddLogViewerCore sets sink.Options and registers IOptions<LogViewerOptions> without touching BaseLogger statics
4. BaseLoggerProviderOptions.cs is marked [Obsolete] (not deleted)
5. LogEventArgs reads format from sink.Options with static fallback
6. LogControlViewModel sets MaxLogSize from sink.Options in constructor
7. Initialize, Shutdown, CreateLogger, CreateLogger<T> carry [Obsolete] attribute
8. All pre-existing tests (≥125) continue to pass
</success_criteria>

<output>
Create `.planning/phases/03-global-state-elimination/03-01-SUMMARY.md` when done.
</output>
